name: test-project
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print hello
        run: echo "Hello World"

      - name: Print Prem
        run: echo "Hello prem"

